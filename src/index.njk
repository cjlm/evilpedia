---
title: Bookworm stats
layout: base.njk
description: "The index page of my website"
---

    <div class="header">

        <h1>{{ title }}</h1>
        <div role="doc-subtitle">
            This website is a fan project for the <a href="https://bookworm.fm/">Bookworm Podcast</a>.<br><br>Each episode Joe and Mike read a book, discuss ways to apply the author's lessons to their lives and give it a rating. All of those ratings are listed below for an easy overview!
        </div>
    </div>

    <div id="episodes" class="sheet">
            <h2>Episodes</h2>
            <div role="h2-subtitle">Find the ratings for each episode below. <br>The table is sortable by clicking on the column titles.</div>
        <table class="table-sortable">
            <thead>
                <tr>
                    <th>No</th>
                    <th>Title</th>
                    <th>Mike's Rating</th>
                    <th>Joe's Rating</th>
                    <th><span class="combined-desktop">Combined</span ><span class="combined-mobile">Avg</span></th>
                </tr>
            </thead>

            <tbody>
                {% block tbody %}
                    {% for i in bwFeed.episodes %}
                        <tr>
                            <td>{{i.no | safe}}</td>
                            <td class="link-cell">
                                <a href="{{i.link}}">{{i.title | safe}}</a>
                            </td>
                            <td class="rating-cell">
                                <span class="rating-no">{{i.ratingMike}}</span>
                                <span class ="rating-stars">{{i.starsMike | safe}}</span>
                            </td>
                            <td class="rating-cell">
                                <span class="rating-no">{{i.ratingJoe}}</span>
                                <span class ="rating-stars">{{i.starsJoe | safe}}</span>
                            </td>
                            <td>{{i.ratingAve}}</td>
                        </tr>
                    {% endfor %}
                {% endblock %}
            </tbody>

        </table>
    </div>
    <div id="trivia" class="trivia">
        <h2>Bookworm Trivia</h2>
        <p role="h2-subtitle">Fun facts and trivia about the podcast.</p>
        <div class="card-container">
            <div class="card-item five-stars">
                <i class="fa-solid fa-medal"></i>
                <p>{{ bwFeed.fiveStars | safe}}</p>
            </div>
            <div class="card-item mike-average">
                <i class="fa-solid fa-medal"></i>
                <p>{{ bwFeed.mikeAvg | safe}}</p>
            </div>
            <div class="card-item joe-average">
                <p>{{ bwFeed.joeAvg | safe}}</p>
            </div>
            <div class="card-item difference">
                <p>{{ bwFeed.biggestDiff | safe }}</p>
            </div>
        </div>
    </div>
<script src="js/tablesort.js"></script>