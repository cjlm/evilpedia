---
title: Evilpedia
layout: base.njk
description: "Index of all the Evil Men covered on the podcast"
---

<div class="header">
  <h1>{{ title }}</h1>
  <div role="doc-subtitle">
  A fan site for the hit comedy podcast Evil Men. All the scores! All the eps!
  </div>
</div>
<div id="episodes" class="sheet">
  <h2>Episodes</h2>
  <table class="table-sortable">
    <thead>
      <tr>
        <th>No</th>
        <th>Title</th>
        <th class="rating">Chris</th>
        <th class="rating">Michael</th>
        <th class="rating">James</th>
        <th class="rating">Guest</th>
        <th>
          <span class="combined-desktop">Combined</span
          ><span class="combined-mobile">Avg</span>
        </th>
      </tr>
    </thead>

    <tbody>
      {% block tbody %} {% for ep in evilMenFeed.episodes %}
      {% set ratings = evilometer[ep.no] %}
      <tr>
        <td class="link-cell"><a href="{{ep.link}}">{{ep.no | safe}}<a></td>
        <td class="link-cell">
          {{ep.title | safe}}
        </td>
        <td class="rating-cell">
          <span class="rating-no" style='background-color: {{ ratings.chrisColor if ratings.chris }}'>{{ratings.chris | nan | safe}}</span>
        </td>
        <td class="rating-cell" >
          <span class="rating-no" style='background-color: {{ ratings.michaelColor if ratings.michael }}'>{{ratings.michael | nan | safe}}</span>
        </td>
        <td class="rating-cell" >
          <span class="rating-no" style='background-color: {{ ratings.jamesColor if ratings.james }}'>{{ratings.james | nan | safe}}</span>
        </td>
        <td class="rating-cell">
          <span class="rating-no">{{ratings.guest}}</span>
          {# <span class="rating-stars">{{ep.guest}}</span> #}
        </td>
        <td>{{ratings.avg | round(2) | nan }}</td>
      </tr>
      {% endfor %} {% endblock %}
    </tbody>
  </table>
</div>
<div id="trivia" class="trivia">
  <h2>Evil Trivia</h2>
  <p role="h2-subtitle">Fun facts and trivia about the podcast.</p>
  <div class="card-container">
    <div class="card-item five-stars">
      <i class="fa-solid fa-medal"></i>
      <p>{{ evilMenFeed.fiveStars | safe}}</p>
    </div>
    <div class="card-item mike-average">
      <i class="fa-solid fa-glasses"></i>
      <p>{{ evilMenFeed.mikeAvg | safe}}</p>
    </div>
    <div class="card-item joe-average">
      <i class="fa-solid fa-pen-nib"></i>
      <p>{{ evilMenFeed.joeAvg | safe}}</p>
    </div>
    <div class="card-item difference">
      <i class="fa-regular fa-comments"></i>
      <p>{{ evilMenFeed.biggestDiff | safe }}</p>
    </div>
  </div>
</div>
<script src="js/tablesort.js"></script>
