<div id="stats" class="stats">
  <h2>Evil Stats</h2>
  <div class="card-container">
    <div class="card-item chart"><img src="/scatter.svg" alt="A scatterplot showing various scores given by the hosts of Evil Men" loading="lazy"/></div>
    <div class="card-item average">
      <div class="icon">{% include 'parrot-head.svg' %}</div>
      <p>Chris's average rating is {{ evilMenFeed.stats.chris.mean | round(2) }}</p>
      <p>The highest score he's given is {{ evilMenFeed.stats.chris.max}}</p>
    </div>
    <div class="card-item average">
      <div class="icon">{% include 'praying-mantis.svg' %}</div>
      <p>Michael's average rating is {{ evilMenFeed.stats.michael.mean | round(2) }}</p>
      <p>The highest score he's given is {{ evilMenFeed.stats.michael.max}}</p>
    </div>
    <div class="card-item average">
      <div class="icon">{% include 'labrador-head.svg' %}</div>
      <p>James's average rating is {{ evilMenFeed.stats.james.mean | round(2) }}</p>
      <p>The highest score he's given is {{ evilMenFeed.stats.james.max}}</p>
    </div>
    <div class="card-item chart"><img src="/chart.svg" alt="A histogram plot showing the sum of scores for each evil man" loading="lazy"/></div>
  </div>
</div>

